# Repo root = pacific-dataviz-2026
# App lives in /web — build with Vite, publish dist (not src).

[build]
  base = "web"
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
